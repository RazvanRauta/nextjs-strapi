query Article($id: ID) {
  newsPost(id: $id) {
    data {
      id
      attributes {
        title
        slug
        text
        date
        image {
          data {
            attributes {
              formats
              width
              height
            }
          }
        }
      }
    }
  }
}
