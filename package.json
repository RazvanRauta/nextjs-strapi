{
  "scripts": {
    "setup:yarn": "yarn && concurrently setup:yarn:*",
    "setup:yarn:backend": "cd backend && yarn",
    "setup:yarn:frontend": "cd web-ui && yarn",
    "develop:yarn": "yarn concurrently develop:yarn:*",
    "develop:yarn:backend": "cd backend && yarn develop",
    "develop:yarn:frontend": "cd web-ui && yarn develop",
    "build:yarn": "cd web-ui && yarn build"
  },
  "devDependencies": {
    "concurrently": "^6.4.0"
  },
  "license": "MIT"
}