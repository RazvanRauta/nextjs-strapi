{
  "scripts": {
    "setup:yarn": "yarn && concurrently 'yarn:setup-*'",
    "setup-yarn-backend": "cd backend && yarn",
    "setup-yarn-frontend": "cd web-ui && yarn",
    "develop:yarn": "concurrently 'yarn:develop-*'",
    "develop-yarn-backend": "cd backend && yarn develop",
    "develop-yarn-frontend": "cd web-ui && yarn dev",
    "build:yarn": "cd web-ui && yarn build"
  },
  "devDependencies": {
    "concurrently": "^6.4.0"
  },
  "license": "MIT"
}